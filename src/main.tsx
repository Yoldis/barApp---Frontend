import React from 'react'
import ReactDOM from 'react-dom/client'
import BarApp from './BarApp.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BarApp />
  </React.StrictMode>,
)
